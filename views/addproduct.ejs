<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            background: #f4f7fc;
        }

        .sidebar {
            height: 100vh;
            width: 270px;
            background: linear-gradient(45deg, #2a9d8f, #264653);
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            padding-top: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
        }

        .sidebar h3 {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 40px;
            color: #e9c46a;
        }

        .nav-item a {
            color: white;
            font-size: 20px;
            padding: 12px 20px;
            margin: 10px 0;
            display: flex;
            align-items: center;
            gap: 15px;
            border-radius: 10px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .nav-item a:hover {
            background: rgba(233, 196, 106, 0.9);
            color: #264653;
            font-weight: bold;
        }

        .nav-item a i {
            font-size: 22px;
        }

        .main-content {
            margin-left: 280px;
            padding: 30px;
        }

        .header {
            background: #e9c46a;
            color: #264653;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .header h1 {
            font-size: 36px;
            font-weight: 700;
            margin: 0;
        }

        .logout-btn {
            background-color: #e76f51;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #e63946;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
            transform: translateY(-3px);
        }

        .product-form {
            background: #ffffff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            animation: fadeIn 1.2s ease-out;
        }

        .product-form h2 {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 20px;
            color:  #264653;
        }

        .form-control {
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 15px;
        }

        .form-control:focus {
            box-shadow: 0 0 8px rgba(106, 15, 118, 0.5);
            border-color: #d95077;
        }

        .form-label {
            font-weight: 600;
            color: #333;
            margin-bottom: 10px;
        }

        .submit-btn {
            background-color: #28a745;
            color: white;
            padding: 12px 20px;
            font-size: 18px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .submit-btn:hover {
            background-color: #218838;
            transform: translateY(-2px);
        }

        /* Animation Keyframes */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(30px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

    </style>
</head>

<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>Farmer Dashboard</h3>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/products"><i class="fas fa-leaf"></i> Products</a>
            </li>
            <li class="nav-item">
                <a href="/purchases"><i class="fas fa-shopping-cart"></i> My Orders</a>
            </li>
            <li class="nav-item">
                <a href="/marketprice"><i class="fas fa-chart-line"></i> Market Prices</a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <h1>Add New Product</h1>
            <button class="logout-btn">Logout</button>
        </div>

        <!-- Product Form -->
        <div class="product-form">
            <h2>Add a New Organic Product</h2>
            <form action="/addproduct" method="post" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="productName" class="form-label">Product Name</label>
                    <input name="productname" type="text" class="form-control" id="productName" placeholder="Enter Product Name" required>
                </div>
                <div class="mb-3">
                    <label for="productDescription" class="form-label">Product Description</label>
                    <textarea name="productDescription" class="form-control" id="productDescription" rows="3" placeholder="Describe the product" required></textarea>
                </div>
                <div class="mb-3">
                    <label for="productPrice" class="form-label">Price (₹)</label>
                    <input name="productPrice" type="number" class="form-control" id="productPrice" placeholder="Enter Price" required>
                </div>
                <div class="mb-3">
                    <label for="productQuantity" class="form-label">Quantity Available</label>
                    <input name="productQuantity" type="number" class="form-control" id="productQuantity" placeholder="Enter Quantity" required>
                </div>
                <div class="mb-3">
                    <label for="productImage" class="form-label">Upload Image</label>
                    <input name="productImage" type="file" class="form-control" id="productImage" required>
                </div>
                <button type="submit" class="submit-btn">Add Product</button>
            </form>
        </div>
    </div>
    <% if (typeof datafound !== 'undefined') { %>
        <script>
          alert("<%= datafound %>");
        </script>
      <% } %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
