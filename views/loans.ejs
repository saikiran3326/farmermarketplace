<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loans & Benefits - Farmer Marketplace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            background-color: #f3f4f6;
        }

        /* Sidebar styling */
        .sidebar {
            height: 100vh;
            width: 270px;
            background: linear-gradient(45deg, #2a9d8f, #264653);
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            padding-top: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
        }

        .sidebar h3 {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 40px;
            color: #e9c46a;
        }

        .nav-item a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            transition: all 0.3s ease;
            border-radius: 8px;
        }

        .nav-item a:hover {
            background: rgba(233, 196, 106, 0.9);
            color: #264653;
            font-weight: bold;
        }

        /* Main content styling */
        .main-content {
            margin-left: 270px;
            padding: 20px;
        }

        .header {
            background: #e9c46a;
            color: #264653;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            font-size: 30px;
            margin: 0;
        }

        .logout-btn {
            background-color: #e76f51;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #d35400;
        }

        /* Loans & Benefits Sections */
        .loans-benefits-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin-top: 20px;
        }

        .loan-box, .benefit-box {
            background: linear-gradient(135deg, #e9c46a, #f4a261);
            color: #264653;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .loan-box:hover, .benefit-box:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .loan-box h3, .benefit-box h3 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .loan-box p, .benefit-box p {
            font-size: 18px;
            margin: 0;
        }

        .loan-box .btn, .benefit-box .btn {
            background-color: #264653;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 16px;
            margin-top: 10px;
            display: inline-block;
            transition: background-color 0.3s ease;
        }

        .loan-box .btn:hover, .benefit-box .btn:hover {
            background-color: #e76f51;
        }

        .loan-box .btn:disabled, .benefit-box .btn:disabled {
            background-color: #9e9e9e;
            cursor: not-allowed;
        }

        /* Status Badge */
        .status-badge {
            font-size: 16px;
            font-weight: bold;
            margin-top: 15px;
            display: inline-block;
            padding: 5px 10px;
            border-radius: 5px;
        }

        .status-applied {
            background-color: #ff9800;
            color: white;
        }

        .status-under-process {
            background-color: #2196f3;
            color: white;
        }

        .status-not-applied {
            background-color: #9e9e9e;
            color: white;
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>Farmer Dashboard</h3>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/marketprice"><i class="fas fa-chart-line"></i> Market Prices</a>
            </li>
            <li class="nav-item">
                <a href="/addproduct"><i class="fas fa-plus-circle"></i> Add Products</a>
            </li>
            <li class="nav-item">
                <a href="#purchase-history"><i class="fas fa-history"></i> Purchase History</a>
            </li>
            <li class="nav-item">
                <a href="/fertilizers"><i class="fas fa-seedling"></i> Fertilizers</a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header Section -->
        <div class="header">
            <h1>Loans & Benefits</h1>
            <button class="logout-btn">Logout</button>
        </div>

        <!-- Loans & Benefits Content -->
        <div class="loans-benefits-container">
            <!-- Loan Options Section -->
             <%loans.forEach(loan=>{ %>
            <div class="loan-box">
                <h3><%=loan.name%></h3>
                <p><%=loan.description%></p>
                <a href=<%=loan.url%> class="btn" disabled>Apply Now</a>
                <span class="status-badge status-<%= loan.status %>">Status: <%= loan.status %></span>
            </div>
<%})%>
        </div>
    </div>

</body>
</html>
