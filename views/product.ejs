<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            background-color: #f3f4f6;
        }

        /* Sidebar styling */
        .sidebar {
            height: 100vh;
            width: 270px;
            background: linear-gradient(45deg, #457b9d, #1d3557);
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            padding-top: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
        }

        .sidebar h3 {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 40px;
            color: #f4a261;
        }

        .nav-item a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            transition: all 0.3s ease;
            border-radius: 8px;
        }

        .nav-item a:hover {
            background: rgba(244, 162, 97, 0.9);
            color: #1d3557;
            font-weight: bold;
        }

        /* Main content styling */
        .main-content {
            margin-left: 270px;
            padding: 20px;
        }

        .header {
            background: #f4a261;
            color: #1d3557;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            font-size: 30px;
            margin: 0;
        }

        .logout-btn {
            background-color: #e63946;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #a11212;
        }

        /* Product Details Card */
        .product-details-card {
            background: white;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            display: flex;
            gap: 20px;
            align-items: center;
        }

        .product-details-card img {
            width: 300px;
            border-radius: 15px;
        }

        .product-info {
            flex: 1;
        }

        .product-info h2 {
            font-size: 28px;
            color: #1d3557;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .product-info p {
            font-size: 16px;
            color: #6c757d;
            margin-bottom: 15px;
        }

        .product-info .price {
            font-size: 24px;
            font-weight: bold;
            color: #f4a261;
            margin-bottom: 20px;
        }

        .actions button {
            background-color: #1d3557;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            margin-right: 10px;
            transition: background-color 0.3s ease;
        }

        .actions button:hover {
            background-color: #e63946;
        }
        .container {
            max-width: 900px;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .product-card {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 20px;
        }

        .product-card img {
            width: 100%;
            max-width: 400px;
            border-radius: 15px;
        }

        .product-info {
            flex: 1;
        }

        .product-info h2 {
            font-size: 28px;
            color: #457b9d;
            font-weight: bold;
        }

        .product-info p {
            font-size: 16px;
            color: #6c757d;
            margin-bottom: 20px;
        }

        .price {
            font-size: 24px;
            font-weight: bold;
            color: #1d3557;
            margin-bottom: 20px;
        }
        .actions{
            display:flex;
        }
        .actions button {
            background-color: #457b9d;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            margin-right: 10px;
            transition: background-color 0.3s ease;
        }

        .actions button:hover {
            background-color: #1d3557;
        }

        .back-btn {
            background-color: #f4a261;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            margin: 20px 0;
            display: inline-block;
            transition: background-color 0.3s ease;
        }

        .back-btn:hover {
            background-color: #e63946;
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>User Dashboard</h3>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/products"><i class="fas fa-leaf"></i> Products</a>
            </li>
            <li class="nav-item">
                <a href="/orders"><i class="fas fa-shopping-cart"></i> My Orders</a>
            </li>
            <li class="nav-item">
                <a href="/market-prices"><i class="fas fa-chart-line"></i> Market Prices</a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header Section -->
        <div class="header">
            <h1>Product Details</h1>
            <button class="logout-btn">Logout</button>
        </div>

        <!-- Product Details Card -->
        <div class="container">
            <!-- Back Button -->
            <button class="back-btn" onclick="history.back()">
                <i class="fas fa-arrow-left"></i> Back to Products
            </button>
    
            <!-- Product Details -->
            <div class="product-card">
                <% if (products[0].img) { %>
                    <img src="<%= products[0].img %>" alt="Product Image">
                <% } else { %>
                    <img src="https://via.placeholder.com/300x200" alt="Placeholder Image">
                <% } %>
                <div class="product-info">
                    <h2><%=products[0].name%></h2>
                    <p>
                       <%=products[0].description%>
                    </p>
                    <div class="price"><%=products[0].price%></div>
                    <div class="actions">
                        <form action="/buyed/<%=products[0].id%>" method="get">
                            <button><i class="fas fa-cart-plus"></i> Add to Cart</button>
                        </form>
                        <form action="/buyed<%=products[0].id%>" method="get">
                            <button ><i class="fas fa-shopping-bag"></i> Buy Now</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>    
    </div>

</body>

</html>
