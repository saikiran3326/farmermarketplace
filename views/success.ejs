<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Success</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            background: #f5f5f7;
            color: #333;
            overflow-x: hidden;
        }

        .sidebar {
            height: 100vh;
            width: 270px;
            background: linear-gradient(45deg, #457b9d, #1d3557);
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            padding-top: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
        }

        .sidebar h3 {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 40px;
            color: #f4a261;
        }

        .nav-item a {
            color: white;
            font-size: 20px;
            padding: 12px 20px;
            margin: 10px 0;
            display: flex;
            align-items: center;
            gap: 15px;
            border-radius: 10px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .nav-item a:hover {
            background: rgba(244, 162, 97, 0.9);
            color: #1d3557;
            font-weight: bold;
        }

        .nav-item a i {
            font-size: 22px;
        }

        .main-content {
            margin-left: 280px;
            padding: 30px;
        }

        .header {
            background: #f4a261;
            color: #1d3557;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 30px;
            margin: 0;
        }

        .logout-btn {
            background-color: #e63946;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #a11212;
        }

        .success-section {
            text-align: center;
            padding: 50px;
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            animation: fadeIn 1.2s ease-out;
        }

        .success-section i {
            font-size: 100px;
            color: #28a745;
            animation: bounce 1.5s infinite;
        }

        .success-section h2 {
            font-size: 38px;
            font-weight: 700;
            color: #28a745;
            margin-top: 20px;
        }

        .success-section p {
            font-size: 20px;
            color: #555;
            margin: 20px 0;
        }

        .order-details {
            background-color: #f7f8fa;
            padding: 25px;
            border-radius: 15px;
            margin-top: 30px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.1);
            font-size: 18px;
        }

        .order-details span {
            font-weight: bold;
            font-size: 20px;
        }

        .order-details .value {
            color: #6a0f76;
            font-size: 22px;
        }

        .back-btn {
            background-color: #457b9d;
            color: white;
            font-size: 18px;
            padding: 14px 25px;
            border-radius: 50px;
            text-decoration: none;
            margin-top: 30px;
            display: inline-block;
            transition: all 0.3s ease;
            animation: pulse 1s infinite;
        }

        .back-btn:hover {
            background-color: #1d3557;
            transform: translateY(-3px);
        }

        .payment-progress {
            margin-top: 20px;
            height: 10px;
            background-color: #e0e0e0;
            border-radius: 50px;
        }

        .payment-progress .progress-bar {
            width: 100%;
            background-color: #28a745;
            border-radius: 50px;
            height: 100%;
        }

        /* Animation Keyframes */
        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20px);
            }
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>User Dashboard</h3>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="#products"><i class="fas fa-leaf"></i> Products</a>
            </li>
            <li class="nav-item">
                <a href="#orders"><i class="fas fa-shopping-cart"></i> My Orders</a>
            </li>
            <li class="nav-item">
                <a href="#market-prices"><i class="fas fa-chart-line"></i> Market Prices</a>
            </li>
            <li class="nav-item">
                <a href="#profile"><i class="fas fa-user"></i> Profile Settings</a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <h1>Payment Success</h1>
            <button class="logout-btn">Logout</button>
        </div>

        <!-- Success Section -->
        <div class="success-section">
            <i class="fas fa-check-circle"></i>
            <h2>Payment Successful</h2>
            <p>Thank you for your purchase! Your payment has been successfully processed.</p>

            <!-- Progress Bar -->
            <div class="payment-progress">
                <div class="progress-bar"></div>
            </div>

            <!-- Order Details -->
            <div class="order-details">
                <span>Order ID: </span><span class="value">1</span><br>
                <span>Order Date: </span><span class="value"><%=date.split('GMT')[0]%></span><br>
                <span>Total Amount: </span><span class="value"><%=price%></span><br>
                <span>Payment Method: </span><span class="value"><%=mode%></span>
            </div>

            <a href="/products" class="back-btn">Go to Dashboard</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
